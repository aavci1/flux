#pragma once

namespace flux {

enum class WindowBackend {
    Auto,       // Automatically select best available backend
    Default     // Default backend (high-quality rendering)
};

} // namespace flux

